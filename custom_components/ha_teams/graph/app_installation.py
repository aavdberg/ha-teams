"""Placeholder: installing/uninstalling the HA app for a Team.

Not implemented yet. This is a prerequisite for the Bot Framework transport
in ``bot/`` and for the Activity Feed transport in ``graph/activity.py``:
Microsoft Graph requires the calling app to be installed for the target
team/user before it can send activity-feed notifications or interactive
bot messages (``/teams/{team-id}/installedApps``,
``/users/{user-id}/teamwork/installedApps``).

See "Deferred Features" in ``.github/copilot-instructions.md`` for why the
Bot Framework and Activity Feed transports were postponed.
"""

from __future__ import annotations
